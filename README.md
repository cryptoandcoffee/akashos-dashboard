# akashos-dashboard

To be run from /home/akash/flask as service with gunicorn in AkashOS.

dev
```
pip install -r requirements.txt
flask run
```

prod
```
pip install -r requirements.txt
gunicorn 
```
